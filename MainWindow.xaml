<Window x:Class="AppBD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppBD"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="1100">
    <Window.Resources>
        <Style x:Key="mainStyle">
            <Setter Property="ItemsControl.Background" Value="#06111C"/>
            <Setter Property="ItemsControl.Foreground" Value="#BEC7C7"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource mainStyle}">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="5 0 5 0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="BorderBrush" Value="#BB2020"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Opacity" Value="0.5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        Background="{TemplateBinding Background}">
                            <ContentPresenter x:Name="contentPresenter" 
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      Content="{TemplateBinding Content}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      Margin="{TemplateBinding Padding}" 
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                    <!--<Setter Property="Foreground" Value="Black"/>-->
                    <!--<Setter Property="BorderBrush" Value="#FFEB4D"/>-->
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type Border}" x:Key="borderStyle">
            <Setter Property="CornerRadius" Value="3" />
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource mainStyle}">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="3" />
                </Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="BorderBrush" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <TabControl TabStripPlacement="Left">
            <TabItem Header="База данных">
                <Grid Style="{StaticResource mainStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="96*"/>
                        <ColumnDefinition Width="317*"/>
                        <ColumnDefinition Width="89*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel x:Name="detailsStackPanel" HorizontalAlignment="Left" Width="105">
                        <Button x:Name="tableButton" Content="Таблицы" Click="tableButton_Click"/>
                        <ListBox Name="tableListBox" SelectionChanged="tableListBox_SelectionChanged" Margin="30 0 5 0" Visibility="Collapsed" Style="{StaticResource mainStyle}"/>
                    </StackPanel>
                    <ScrollViewer Grid.Column="1" x:Name="infoScrollViewer" >
                        <StackPanel x:Name="infoStackPanel" HorizontalAlignment="Center" Grid.Column="1">
                            <TextBlock Name="nameTableTextBlock" FontSize="25" Height="30" Margin="10" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </ScrollViewer>
                    <ScrollViewer x:Name="toolsScroll" Grid.Column="2">
                        <StackPanel x:Name="toolsStackPanel" HorizontalAlignment="Right" Grid.Column="2">
                            <Button x:Name="addButton" Content="Добавить" Click="addButton_Click"/>
                            <StackPanel Margin="30 0 0 0" Name="addStackPanel" Visibility="Collapsed"/>
                            <Button x:Name="editButton" Content="Редактировать" Click="editButton_Click"/>
                            <StackPanel x:Name="editStackPanel" Visibility="Collapsed" Margin="30 0 0 0"/>
                            <Button x:Name="deleteButton" Click="deleteButton_Click" Content="Удалить"/>
                            <Button x:Name="saveButton" Content="Сохранить" Click="saveButton_Click"/>
                            <TextBox x:Name="findTextBox" TextChanged="findTextBox_TextChanged" Text="Поиск..." Opacity="0.5" Width="130" GotFocus="findTextBox_GotFocus" LostFocus="findTextBox_LostFocus"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
